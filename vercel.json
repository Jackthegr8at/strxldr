{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "commands": ["npm run build"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "build/index.html"
    }
  ]
}